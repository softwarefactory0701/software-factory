import assert from"node:assert/strict";import{describe,it}from"node:test";import{groupOpportunitiesByStage}from"@software-factory/crm-core";import{agents,contacts,opportunities,pipeline,properties,tasks,visits}from"./index";
describe("NOVA dataset",()=>{it("keeps the approved commercial scenario",()=>{assert.equal(contacts.length,14);assert.equal(opportunities.length,12);assert.equal(properties.length,9);assert.equal(tasks.length,10);assert.equal(visits.length,7);assert.equal(agents.length,3)});it("only references existing entities",()=>{for(const o of opportunities){assert.ok(contacts.some(c=>c.id===o.contactId));assert.ok(pipeline.stages.some(s=>s.id===o.stageId))}});it("groups the whole pipeline",()=>assert.equal(Object.values(groupOpportunitiesByStage(opportunities,pipeline.stages)).flat().length,12))});
