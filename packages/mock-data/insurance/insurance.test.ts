import assert from"node:assert/strict";import{describe,it}from"node:test";import{contacts,policies,producers,products,opportunities,pipeline,renewals,tasks,activities}from"./index";describe("COVERA scenario",()=>{it("has deterministic approved volume",()=>{assert.equal(contacts.length,14);assert.equal(policies.length,18);assert.equal(products.length,6);assert.equal(producers.length,3);assert.equal(opportunities.length,12);assert.equal(tasks.length,14);assert.equal(activities.length,14)});it("keeps CRM and policy references coherent",()=>{for(const p of policies){assert.ok(contacts.some(c=>c.id===p.contactId));assert.ok(products.some(x=>x.id===p.productId))}for(const o of opportunities)assert.ok(pipeline.stages.some(s=>s.id===o.stageId));for(const r of renewals)assert.ok(policies.some(p=>p.id===r.policyId))})});
