import assert from"node:assert/strict";import{describe,it}from"node:test";import{companies,contacts,opportunities,pipeline,sellers,tasks}from"./index";describe("VANTAGE scenario",()=>{it("keeps the approved deterministic volume",()=>{assert.equal(companies.length,8);assert.equal(contacts.length,14);assert.equal(opportunities.length,14);assert.equal(sellers.length,3);assert.equal(tasks.length,12)});it("keeps neutral references coherent",()=>{for(const o of opportunities){assert.ok(contacts.some(c=>c.id===o.contactId));assert.ok(pipeline.stages.some(s=>s.id===o.stageId));assert.ok(companies.some(c=>c.id===o.metadata?.companyId))}})});
