import assert from"node:assert/strict";import{describe,it}from"node:test";import{buildOpportunityTimeline,createCrmDemoState,crmDemoReducer}from"./demo-state";import type{CrmDemoScenario}from"./demo-state";const scenario:CrmDemoScenario={opportunities:[{id:"o",contactId:"c",pipelineId:"p",stageId:"a",title:"Deal",status:"open",createdAt:"2026-09-01"}],tasks:[{id:"t",opportunityId:"o",title:"Follow",dueAt:"2026-09-08T10:00:00",ownerId:"u",status:"open"}],activities:[]};describe("CRM demo state",()=>{it("selects, moves, wins and resets",()=>{let s=createCrmDemoState(scenario);s=crmDemoReducer(s,{type:"select",opportunityId:"o"});s=crmDemoReducer(s,{type:"move",opportunityId:"o",stageId:"b"});s=crmDemoReducer(s,{type:"set-outcome",opportunityId:"o",status:"won"});assert.equal(s.selectedOpportunityId,"o");assert.equal(s.opportunities[0]?.status,"won");assert.equal(crmDemoReducer(s,{type:"reset",scenario}).opportunities[0]?.stageId,"a")});it("toggles tasks and adds notes",()=>{let s=createCrmDemoState(scenario);s=crmDemoReducer(s,{type:"toggle-task",taskId:"t"});s=crmDemoReducer(s,{type:"add-activity",activity:{id:"n",opportunityId:"o",type:"note",occurredAt:"2026-09-08T12:00:00",summary:"Nota"}});assert.equal(s.tasks[0]?.status,"completed");assert.equal(buildOpportunityTimeline("o",s.activities,s.tasks).length,2)})});
