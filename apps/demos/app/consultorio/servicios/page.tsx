import { nexusProfessionals, nexusServices } from "@software-factory/mock-data/consultorio";
import { StatusBadge } from "@software-factory/ui";
import { NexusPageHeader, NexusPanel, formatNexusCurrency } from "../_components/nexus-primitives";

export default function ConsultorioServicesPage(){return <><NexusPageHeader description="Tipos de consulta, duración estimada y profesionales disponibles." eyebrow="Catálogo administrativo" title="Servicios"/><div className="mb-5 flex gap-2 overflow-x-auto pb-1">{["Todos","Medicina general","Nutrición","Fisioterapia"].map((item,index)=><button className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-semibold ${index===0?"bg-[#263836] text-white":"border border-[#dbe3df] bg-white text-[#60716e]"}`} key={item} type="button">{item}</button>)}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{nexusServices.map(service=><NexusPanel className="p-5" key={service.id}><div className="flex items-center justify-between"><span className="text-[10px] font-semibold uppercase tracking-[.13em] text-[#347b78]">{service.category}</span><StatusBadge tone="success">Activo</StatusBadge></div><h3 className="mt-5 text-xl font-semibold text-[#263432]">{service.name}</h3><div className="mt-4 flex items-baseline gap-3"><p className="text-2xl font-semibold">{formatNexusCurrency(service.price)}</p><span className="text-xs text-[#82908d]">{service.durationMinutes} min</span></div><div className="mt-5 border-t border-[#e5ebe8] pt-4"><p className="text-[10px] uppercase tracking-[.12em] text-[#82908d]">Profesionales disponibles</p><p className="mt-2 text-sm text-[#526663]">{service.professionalIds.map(id=>nexusProfessionals.find(item=>item.id===id)?.name).join(" · ")}</p></div></NexusPanel>)}</div></>}
