import type { ReactNode } from "react";
export const money=(value:number)=>new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS",maximumFractionDigits:0}).format(value);
export function PageIntro({eyebrow,title,description,action}:{readonly eyebrow:string;readonly title:string;readonly description:string;readonly action?:ReactNode}){return <div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-teal-700">{eyebrow}</p><h2 className="mt-2 text-3xl font-bold tracking-tight text-[#15242d] sm:text-4xl">{title}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">{description}</p></div>{action}</div>}
export function Panel({children,className=""}:{readonly children:ReactNode;readonly className?:string}){return <section className={`min-w-0 rounded-2xl border border-slate-200/80 bg-white shadow-[0_12px_35px_rgba(15,35,45,.05)] ${className}`}>{children}</section>}
export function StatusPill({status}:{readonly status:"ok"|"low"|"out"}){const map={ok:["Correcto","bg-teal-50 text-teal-700 border-teal-200"],low:["Reponer","bg-amber-50 text-amber-700 border-amber-200"],out:["Agotado","bg-red-50 text-red-700 border-red-200"]} as const;return <span className={`inline-flex w-fit justify-self-start rounded-full border px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide ${map[status][1]}`}>{map[status][0]}</span>}
export function Bar({value,tone="teal"}:{readonly value:number;readonly tone?:"teal"|"amber"|"navy"}){return <div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${tone==="amber"?"bg-amber-400":tone==="navy"?"bg-slate-700":"bg-teal-500"}`} style={{width:`${Math.min(value,100)}%`}}/></div>}
