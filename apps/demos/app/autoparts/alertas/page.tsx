import{partViews}from"../_components/partx-data";import{Intro,StockTag,Surface}from"../_components/partx-ui";export default function Page(){const alerts=partViews.filter(x=>x.status!=="ok").sort((a,b)=>a.status===b.status?0:a.status==="out"?-1:1);return <><Intro kicker="Ventas en riesgo" title="Alertas" text="Prioridad comercial, demanda y tiempo estimado de reposición."/><div className="grid gap-4 lg:grid-cols-2">{alerts.map(x=>{const critical=x.status==="out";return <Surface className={`border-l-4 p-5 ${critical?'border-l-red-500':'border-l-amber-400'}`} key={x.product.id}><div className="flex justify-between gap-3"><div><p className={`text-[10px] font-black uppercase tracking-widest ${critical?'text-red-700':'text-amber-700'}`}>{critical?'Crítico':'Alto'}</p><p className="mt-2 text-xs font-black text-[#28738a]">{x.metadata.brand} · {x.metadata.partNumber}</p><h3 className="mt-1 font-bold">{x.product.name}</h3></div><StockTag status={x.status}/></div><div className="mt-4 grid grid-cols-3 gap-3 text-xs"><div><span className="text-slate-400">Demanda</span><b className="block">{x.metadata.demand==="high"?'Alta':'Media'}</b></div><div><span className="text-slate-400">Última salida</span><b className="block">Hoy 10:51</b></div><div><span className="text-slate-400">Reposición</span><b className="block">{x.supplier?.leadTimeDays??4} días</b></div></div><p className="mt-4 text-xs text-slate-500">{x.total} disponibles · Mínimo {x.minimum} · {x.supplier?.name}</p><button className="mt-4 bg-[#20282d] px-3 py-2 text-xs font-bold text-white">Gestionar reposición</button></Surface>})}</div></>}
