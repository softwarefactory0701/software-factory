"use client";
import{useEffect,useState}from"react";import{Container}from"../ui/primitives";
const links=[['Soluciones','#soluciones'],['Demos','#demos'],['Precios','#precios'],['Software a medida','#software-medida']]as const;
export function SiteHeader(){const[scrolled,setScrolled]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);onScroll();window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);return <header className={`site-header ${scrolled?'is-scrolled':''}`}><Container wide className="header-inner"><a className="brand" href="#top" aria-label="Software Factory, inicio"><span className="brand-mark">S/F</span><span>Software Factory</span></a><nav aria-label="Navegación principal" className="desktop-nav">{links.map(([label,href])=><a href={href} key={href}>{label}</a>)}</nav><a className="button button--ink header-cta" href="#contacto">Hablemos <span aria-hidden>↗</span></a><button aria-expanded={open} aria-label={open?'Cerrar menú':'Abrir menú'} className="menu-button" onClick={()=>setOpen(value=>!value)}><span/><span/></button></Container><div className={`mobile-menu ${open?'is-open':''}`} aria-hidden={!open}><nav aria-label="Navegación móvil">{links.map(([label,href],index)=><a href={href} key={href} onClick={()=>setOpen(false)}><small>0{index+1}</small>{label}</a>)}<a href="#contacto" onClick={()=>setOpen(false)}><small>05</small>Hablemos</a></nav><p>Software pensado para negocios reales.</p></div></header>}
